@{
    Layout = null;
}


    <div>
        <div>Description:</div>
        <h1>@ViewBag.SeriesTitle</h1>

        <div>Issue Number:</div>
        <h3>@ViewBag.IssueNumber</h3>

        <div>@Html.Raw(ViewBag.Description)</div>

        @if(ViewBag.Artists.Length > 0)
            {
                <h5>Artists:</h5>
                <div>
                    <ul>
                        @foreach (var artist in ViewBag.Artists)
                        {
                            <li>@artist</li>
                        }
                        
                    </ul>
                </div>
            }
    </div>

